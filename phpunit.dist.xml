<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <!-- Both `env` AND `server` are required, not just one: the dev container sets the real
             OS env var APP_ENV=dev (docker/, ENV APP_ENV=dev), which PHP's CLI SAPI imports
             into $_ENV before any userland code runs. Symfony\Component\Dotenv\Dotenv::loadEnv()
             picks the active env from $_SERVER first, while KernelTestCase::createKernel() picks
             it from $_ENV first — so overriding only one of the two superglobals here left the
             other still reading "dev" from the OS, and tests silently booted the dev container
             (no `test.service_container`, real external HTTP calls instead of the fakes in
             config/services_test.yaml) or loaded .env.dev instead of .env.test. -->
        <env name="APP_ENV" value="test" force="true" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <testsuite name="functional">
            <directory>tests/Functional</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
        <!-- Wraps every test in a transaction that's rolled back at the end, so integration
             and functional tests hitting real PostgreSQL never leak state between tests or
             need manual cleanup. -->
        <bootstrap class="DAMA\DoctrineTestBundle\PHPUnit\PHPUnitExtension"/>
    </extensions>
</phpunit>
